<!DOCTYPE html>
<html lang="en">
<head>
  <title>BirdLifeList</title>
  <meta charset="utf-8">
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f5f5f5;
      margin: 0;
      padding: 0;
    }

    header {
      background-color: #4CAF50;
      color: #fff;
      padding: 20px;
      display: flex;
      align-items: center;
      justify-content: space-between; /* Align items to the start and end of the header */
    }

    h1 {
      margin: 0;
      font-size: 28px;
    }

    .action-buttons {
      display: flex;
      flex-direction: column; /* Stack buttons vertically */
      align-items: flex-end; /* Align buttons to the right */
    }

    .action-buttons a, .action-buttons button {
      background-color: #fff;
      color: #4CAF50;
      border: 1px solid #4CAF50;
      padding: 10px 30px;
      border-radius: 20px;
      cursor: pointer;
      text-decoration: none;
      font-size: 16px;
      margin-top: 10px; 
      transition: all 0.3s ease;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      width: 180px;
      text-align: center;
    }

    .action-buttons a:hover, .action-buttons button:hover {
      background-color: #e0e0e0;
      transform: translateY(-5px);
    }

    .binoculars-logo {
      width: 50px;
      height: auto;
      margin-right: 20px;
      filter: invert(100%);
    }

    main {
      padding: 20px;
    }

    footer {
      color: #ccc;
      padding: 10px 20px;
      text-align: left;
    }

  </style>
</head>
<body>

<header>
  <div class="logo-and-title">
    <img class="binoculars-logo" src="/images/binoculars.svg" alt="Binoculars Logo">
    <h1>BirdLifeList</h1>
  </div>
  <div class="action-buttons">
    <a href="/users/signout">Sign Out</a>
    <a href="/birds/new">Add new species</a>
  </div>
</header>

<main>
  <% if @session_persistence.message %>
  <div>
    <p><%== @session_persistence.delete_key(:message) %></p>
  </div>
  <% end %>
  <%== yield %>
</main>

<footer>
  <p>&copy; 2024 BirdLifeList. All rights reserved.</p>
</footer>

</body>
</html>
